<%= form_with(model: [:admin, @menu]) do |form|  %>
    <% if @menu.errors.any? %>
        <div style="color: red">
            <h2><%= pluralize(@menu.errors.count, "error") %> prohibited this page from being saved:</h2>

            <ul>
                <% @menu.errors.each do |error| %>
                    <li> <%= error.full_message %> </li>
                <% end%>
            </ul>
        <div>
    <% end %> 
   <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
    <h3> Menu items </h3>
    <%= form.fields_for :menu_items do |c| %>
        <p>
            <%= c.text_field :title %>
            <%= c.text_field :url %>
            <%= c.hidden_field :id %>
        </p>
    <%end%>
  </div>
    <div>
        <%= form.submit %>
    </div>
<%end%>

